# Data

This directory is for local datasets and generated synthetic data.

Do not commit raw highD, inD, rounD, CARLA logs, or other large/restricted datasets. The initial tests and baseline demo use synthetic data only.
